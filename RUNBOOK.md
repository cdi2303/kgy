# 검증 런북 — 시나리오별 행동

게시 후 결과에 따라 **무엇을 할지** 결정하는 트리. 판단 근거: `GTM.md §4`(신호의 질) + `/api/stats`(정량).
측정 창: **첫 게시 후 2주**. 정량은 보조, **인터뷰(정성)가 본 신호**.

> ⚠️ 정량 숫자는 봇/새로고침 포함 rough. 단독 판단 금지. 인터뷰 결과가 우선.

---

## 신호 어떻게 확인하나

- **어드민 대시보드**: `https://cronwatch-production.up.railway.app/admin`
  → `ADMIN_TOKEN` 입력. 방문/가입/전환 + **채널별 가입(ref)** + **최근 가입(이메일 마스킹)** 표시.
  토큰 확인: 터미널 `railway variables` 에서 `ADMIN_TOKEN` 값 복사.
- **빠른 숫자만**: `GET /api/stats` (공개, 숫자만) — `{landing_views, signups, conversion}`.
- **봇 vs 진짜 구분**: 어드민의 최근 가입 도메인 보면 판단. `@example.com` 류 = 테스트/봇.
- **판단**: 숫자는 보조. 본 신호는 **인터뷰**(아래 시나리오).

## 0. 지금 즉시 (게시 전/직후)

1. `GTM.md` 포스트 3종 본인 목소리로 다듬기.
2. 채널별 `?ref=` 링크 준비 (`GTM.md §4-1`).
3. 화~목 오전(한국시간) **GeekNews 1곳부터** 게시. 한 번에 다 뿌리지 말 것(채널별 반응 비교).
4. 게시 직후 **1시간 댓글 응대**.
5. 가입 들어오면 즉시 **인터뷰 요청**(Mom Test 5질문, `GTM.md §3`).
6. 매일 `GET /api/stats`로 추세 기록.

---

## 시나리오 A — 강신호 → Stage 2 진입

**트리거 (둘 다 충족):**
- 정량: 가입 절대수 의미있음(목표 예: 2주 20+), 방문 대비 전환 대략 3%+.
- 정성(**핵심**): 인터뷰 5명 중 **3명 이상**이 ① 실제 과거 배치 사고 + ② 현재 감시 불편 + ③ 관련 지출/노력 이력.

**행동:**
1. **알림톡 게이트 먼저** — 카카오 비즈채널 개설 + 템플릿 1건 등록 + 심사 제출(리드타임 있음). `ALIMTALK.md §2`.
2. 심사 대기 중 병행 개발 착수(Claude와): 인증/멀티테넌시 → 이메일(SMTP) + 카카오워크 → 알림톡(Solapi).
3. 가격 검증: 인터뷰서 "과거 유사 도구 지출액" 기반으로 `GTM.md` 가격 가설 조정.

→ 이 시점에 Claude에게 "신호 떴다, Stage 2 가자" 하면 ROADMAP Stage 2 시작.

---

## 시나리오 B — 약신호 → 조정(피벗 아님)

**트리거:** 가입은 좀 되는데 전환 낮거나(<1~2%), 인터뷰서 "있으면 좋고" 수준(과거 페인·지출 없음).

**행동 (싸게 빠르게 반복):**
1. 가설 점검: 틈새가 너무 넓나? 헤드라인이 진짜 페인을 안 건드리나?
2. **랜딩 헤드라인/오퍼 교체** 후 다른 채널 재게시 (A/B 식 비교).
3. **타깃 더 좁히기** — 예: "특정 스택(Laravel/Django 스케줄러)" 또는 "특정 업종 정산배치"로 메시지 특화.
4. 인터뷰 n 늘려 공통 페인 패턴 탐색.
- 코드 변경 최소(랜딩 카피 위주). DB/기능 건드리지 말 것.

---

## 시나리오 C — 무반응 → 원인 분리 후 재시도/피벗

**먼저 `/api/stats`로 원인 분리:**

- **views 낮음(도달 실패):** 채널/노출 문제.
  → 다른 커뮤니티(OKKY·디스코드·X), 다른 시간대, 제목 바꿔 재시도. 메시지 문제 아닐 수 있음.
- **views 있는데 signups 0(전환 실패):** 메시지/가치/오퍼 문제.
  → 헤드라인·CTA 재설계. 그래도 0이면 **수요 자체 의심** → 시나리오 D-피벗 검토.

---

## 시나리오 D — 알림톡 심사 반려 (Stage 2 게이트 실패)

**트리거:** 템플릿이 카카오 "정보성" 기준 미충족으로 반려.

**행동:**
1. 반려 사유 확인 → 템플릿 문구 수정(순수 상태통보형, 홍보문구 0) 재심사.
2. 재반려 시 **채널 포지셔닝 조정**: 개인=SMS(종량, 즉시) + 팀=카카오워크(무료, 구현됨)로 가치 유지.
3. "카톡 알림" 헤드라인을 "즉시 장애 알림(SMS·카카오워크)"으로 완화. `ALIMTALK.md §4`.
- 차별화 약화되므로, 이 경우 가격/포지셔닝 재검토 필요.

---

## 시나리오 E — 비용 대비 신호 미미 → 일시중단

**트리거:** 2~4주 반복에도 신호 약함 + Railway 비용 부담.

**행동:**
1. 랜딩을 정적 호스팅(무료)으로 옮기거나 서비스 일시중단(데이터 백업: `railway volume files download`는 SSH 키 필요).
2. 학습 기록(왜 안 됐나) 남기고 다른 아이디어로.
- 코드/문서는 리포지토리에 남으니 나중에 재개 가능.

---

## 판단 요약 (한 줄)

> **인터뷰서 "과거에 이 문제로 실제 고생/지출했다"가 반복되면 강신호 → Stage 2.
> "있으면 좋겠네"만 나오면 약신호 → 조정. 그 차이가 전부다.** (Mom Test)
